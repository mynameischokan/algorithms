/*
 *  INFO
 *      Best case of `binary search`: search that matches `root elem`.
 *      Best case time => B(n) = 1
 *
 *      Worst case of `binary search`: search for match at `leaf elem`.
 *      Worst case time => W(n) = h(height)
 *
 *      Average case of `binary search`: depends on the length(height) of tree
 *      Average case time => min W(n) = logn
 *      Average case time => max W(n) = n
 */

int main() {

}